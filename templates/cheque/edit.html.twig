{% extends 'baseHome.html.twig' %}

{% block title %}Editar Cheque
{% endblock %}

{% block body %}
	<h2>Editar Cheque</h2>
	<section class="content mt-5">
		<div class="container-fluid">
			<div class="row">
				<div class="col-12">
					<div class="card">
						<div class="card-header">
							<h3 class="card-title">Editar Cheque</h3>
						</div>
						<div class="container py-4 px-0">

							{{ include('cheque/_form.html.twig', {'button_label': 'Actualizar'}) }}
							<hr class="mb-3">
							<a href="{{ path('cheques') }}" class="btn btn-primary ">
								<i class="fas fa-angle-left"></i>
							</a>

							{{ include('cheque/_delete_form.html.twig') }}
						</div>
					</div>
					<!--end card-->
				</div>

			</div>
		</div>
	</section>
{% endblock %}

