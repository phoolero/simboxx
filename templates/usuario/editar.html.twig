{% extends 'baseHome.html.twig' %}

{% block title %}SIMBOX - Usuario{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        -ms-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }
	  .jumbotron{
		background-image: url("{{ asset('img/fondos/fondo-jumbotron.jpg')}}");
		min-height:500px;
	  }
	  .notoria{
		font-size: 36px;
		color: white;
	  }
</style>

<main role="main">

  <div class="container pt-4">
	<div class="row" style="margin: 40px 0px 20px 0px">
      <div class="col-md-12">
		<h2><i class="fas fa-user-circle"></i>Datos del Usuario</h2>
 		{{form_start(form)}}
		{{form_widget(form)}}
		{{form_end(form)}} 
	  </div>	  
	</div>

  </div> <!-- /container -->

</main>

{% endblock %}
