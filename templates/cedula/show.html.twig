{% extends 'baseHome.html.twig' %}

{% block title %}Cedula
{% endblock %}

{% block body %}
	<h1>Vista Cedula</h1>
	<section class="content mt-5">
		<div class="container-fluid">
			<div class="row">
				<div class="col-12">
					<div class="card">
						<div class="card-header">
							<h3 class="card-title">Tabla de Cédula</h3>
						</div>
						<div class="card-body">
							<table class="table table-bordered table-hover">
								<tbody>
									<tr>
										<th>Id</th>
										<td>{{ cedula.rut }}</td>
									</tr>
									<tr>
										<th>Ruta_imagen</th>
										<td><img width="250" src="{{ asset('../public/img/carnes/' ~ cedula.rutaImagen )}}"/></td>
									</tr>
								</tbody>
							</table>

							<a href="{{ path('cedulas') }}" class="btn btn-primary ml-2">
								<i class="fas fa-angle-left"></i>
							</a>

							<a href="{{ path('cedula_editar', {'id': cedula.rut}) }}" class="btn btn-warning">
								<i class="fas fa-edit text-white"></i>
							</a>

							{{ include('cedula/_delete_form.html.twig') }}
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>
{% endblock %}

